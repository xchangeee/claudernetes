kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: claudernetes-test
nodes:
  - role: control-plane
    extraPortMappings:
      # ArgoCD UI
      - containerPort: 30080
        hostPort: 8080
        protocol: TCP
      # Test app ingress
      - containerPort: 30443
        hostPort: 8443
        protocol: TCP
